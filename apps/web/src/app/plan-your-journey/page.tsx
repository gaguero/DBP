import type { Metadata } from "next"; import { PageHero } from "@/components/page-hero"; import { Card } from "@/components/card"; import { travelRoutes, faqs } from "@/content/data"; import { Button } from "@/components/button";  export const metadata: Metadata = {   title: "Plan Your Journey",   description:     "Everything you need to reach Dolphin Blue Paradise: travel routes, transfers, packing tips, and FAQs.", };  export default function PlanYourJourneyPage() {   return (     <div className="space-y-24 pb-24">       <PageHero         title="From Airport to Paradise"         kicker="Plan"         description="Our concierge team coordinates each leg of your travel—flights, drivers, and private boat transfers—to make your arrival effortless."         image="/images/hero-bay.jpg"       />        <section className="section">         <div className="container grid gap-8 md:grid-cols-3">           {travelRoutes.map((route) => (             <Card key={route.title} className="space-y-3">               <h2 className="font-display text-xl text-[var(--color-navy)]">{route.title}</h2>               <ul className="space-y-2 text-sm text-muted">                 {route.options.map((option) => (                   <li key={option} className="flex items-start gap-2">                     <span aria-hidden className="mt-1 text-[var(--color-gold)]">•</span>                     <span>{option}</span>                   </li>                 ))}               </ul>             </Card>           ))}         </div>       </section>        <section className="section bg-[var(--color-sand)]">         <div className="container grid gap-10 md:grid-cols-[1.1fr_0.9fr] md:items-start">           <div className="space-y-5">             <h2 className="font-display text-3xl text-[var(--color-navy)]">FAQs</h2>             <div className="space-y-4">               {faqs.map((faq) => (                 <div key={faq.question} className="space-y-2">                   <p className="font-semibold text-[var(--color-navy)]">{faq.question}</p>                   <p className="text-sm text-muted">{faq.answer}</p>                 </div>               ))}             </div>           </div>           <Card className="space-y-3">             <h3 className="text-sm uppercase tracking-[0.3em] text-[var(--color-gold)]">Concierge Support</h3>             <p className="text-sm text-muted">               Share your flight details and we&apos;ll coordinate transfers from Bocas Town or Tierra Oscura within our               scheduled pick-up windows (12:30 pm and 5:00 pm). Late arrivals can be arranged with advance notice.             </p>             <Button href="mailto:contact@dolphinblueparadise.com" className="w-full">               Request Travel Assistance             </Button>             <Button href="https://wa.me/50763460605" variant="secondary" className="w-full">               WhatsApp Concierge             </Button>           </Card>         </div>       </section>     </div>   ); }