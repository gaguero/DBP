import Image from "next/image"; import type { ReactNode } from "react"; import clsx from "clsx";  interface PageHeroProps {   title: string;   kicker?: string;   description?: string;   image?: string;   actions?: ReactNode;   overlayClassName?: string; }  export function PageHero({ title, kicker, description, image, actions, overlayClassName }: PageHeroProps) {   return (     <section className="relative isolate overflow-hidden">       {image ? (         <Image src={image} alt="" fill priority className="object-cover" />       ) : (         <div className="absolute inset-0 bg-gradient-to-br from-[var(--color-navy)] via-[var(--color-ocean)] to-[var(--color-forest)]" />       )}       <div         className={clsx(           "absolute inset-0 bg-gradient-to-b from-black/55 via-black/45 to-black/70",           overlayClassName,         )}       />       <div className="relative container flex min-h-[60vh] flex-col justify-center gap-6 py-24 text-white">         {kicker ? (           <p className="text-sm uppercase tracking-[0.4em] text-[var(--color-gold)]">{kicker}</p>         ) : null}         <h1 className="max-w-3xl font-display text-4xl md:text-6xl">{title}</h1>         {description ? <p className="max-w-2xl text-lg text-white/80">{description}</p> : null}         {actions}       </div>     </section>   ); }